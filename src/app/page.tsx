'use client';

import { Button } from 'goldends';

const HomePage: React.FC = () => {
	return (
		<main className="mx-20 pt-5">
			<p>Initial Page</p>
			<Button text="button test" />
		</main>
	);
};

export default HomePage;
